# 🚀 演示快速参考卡片

**打印这个页面，演示时放在手边！**

---

## 📋 准备工作

```bash
# 1. 确认有训练好的模型
cd /Users/east/AndroidStudioProjects/iotproject/ml_code
ls models/

# 记下模型文件名：
# __________________________________________

# 2. 确认手机连接
/Users/east/Library/Android/sdk/platform-tools/adb devices

# 3. 决定演示密码（推荐）：54321
```

---

## 🎬 演示步骤（5步）

### 1️⃣ 介绍（30秒）
```
"这是一个密码预测系统，
用传感器数据预测密码输入。
模型用 [X] 个样本训练，准确率 [Y]%。"
```

---

### 2️⃣ 收集数据（1分钟）
```
1. 打开手机 App
2. 点击"进入密码预测模式"
3. 让老师按：5-4-3-2-1
4. 点击"完成并保存"
5. 点击"返回"
```

---

### 3️⃣ 导出数据（30秒）
```bash
cd /Users/east/AndroidStudioProjects/iotproject
./export_data.sh
```

---

### 4️⃣ 找到测试数据文件（10秒）
```bash
ls -lt sensor_data/files/ | head -2

# 记下最新文件名：
# __________________________________________
```

---

### 5️⃣ 运行预测（30秒）
```bash
cd ml_code

python 4_predict_password.py \
    --model ./models/[你的模型名].pkl \
    --data ../sensor_data/files/[刚才的文件名].csv \
    --actual 54321
```

**填入**：
- `[你的模型名]` = 第1步记下的模型文件名
- `[刚才的文件名]` = 第4步记下的文件名

---

## 🎤 讲解要点

### 预测成功时
```
"✓ 模型检测到5个按键
✓ 每个数字的置信度都在70-90%
✓ 最终预测密码：54321
✓ 完全正确！"
```

### 预测有误时
```
"✓ 5个数字预测对了4个
⚠ 第X个把Y预测成了Z（相邻数字容易混）
✓ 总体准确率80%
✓ 证明了系统可行性"
```

---

## ❓ 常见问题快速回答

**Q: 准确率？**
```
"测试集准确率 [Y]%
文献类似研究：60-80%"
```

**Q: 原理？**
```
"不同数字按键位置不同
→ 手机倾斜/加速度不同
→ 传感器捕捉这些差异
→ ML模型学习模式"
```

**Q: 真实威胁？**
```
"理论可行，实际需要：
✓ 安装恶意App
✓ 大量训练数据
✓ 跨用户泛化难
防御：生物识别、限制传感器"
```

**Q: 技术栈？**
```
"Android: Kotlin + Compose
传感器: ACC/GYRO/ROT/MAG @50Hz
ML: Python + Random Forest
特征: 200+ (时域+频域)"
```

---

## 🚨 应急预案

**App崩溃**：
```bash
# 用之前的数据演示
python 4_predict_password.py \
    --model ./models/[模型名].pkl \
    --data ../sensor_data/files/[旧数据].csv \
    --actual [旧密码]
```

**准确率低**：
```
"这说明跨用户泛化困难，
也是一个有价值的发现！"
```

---

## ✅ 检查清单

演示前：
```
□ 模型文件名已记录
□ 演示密码已决定：____________
□ 手机已连接
□ 完整彩排过1次
□ 终端字体够大
□ 电量充足
```

---

## 📞 紧急命令

```bash
# 快速查看最新文件
ls -t sensor_data/files/*.csv | head -1

# 快速找到最新模型
ls -t ml_code/models/*.pkl | head -1

# 完整预测命令模板
cd ml_code && python 4_predict_password.py \
    --model $(ls -t models/*.pkl | head -1) \
    --data $(ls -t ../sensor_data/files/*.csv | head -1) \
    --actual 54321
```

---

**祝你演示成功！🎉**
